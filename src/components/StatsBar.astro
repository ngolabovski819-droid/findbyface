---
import { localeOf } from '../i18n/routes';
import { t } from '../i18n/ui';

const lang = localeOf(Astro.url.pathname);
const stats = [
  { number: '2.4M+', label: t(lang, 'stats.creators') },
  { number: '98%',   label: t(lang, 'stats.accuracy') },
  { number: '<2s',   label: t(lang, 'stats.searchTime') },
  { number: t(lang, 'stats.free'), label: t(lang, 'stats.toStart') },
];
---

<div class="stats-bar">
  {stats.map(s => (
    <div class="stat">
      <span class="stat-number">{s.number}</span>
      <span class="stat-label">{s.label}</span>
    </div>
  ))}
</div>

<style>
  .stats-bar {
    display: flex;
    justify-content: center;
    gap: 48px;
    padding: 40px 20px;
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .stat-number {
    font-family: 'Manrope', sans-serif;
    font-weight: 800;
    font-size: 2rem;
    background: linear-gradient(135deg, #7c3aed, #ec4899);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .stat-label {
    font-size: 11px;
    font-weight: 600;
    color: var(--text-muted);
    letter-spacing: 0.08em;
  }
</style>
